#include "reactor/wifi_event_update.h"

#include "data/wifi_data.h"

void* wifi_event_update(void *arg)
{
    WIFI *wifi = (WIFI *) arg;
    (void)wifi;

    while (1)
    {

    }

    return NULL;
}
